{% trans_default_domain 'FOSUserBundle' %}

{{ form_start(form, { 'action': path('fos_user_profile_edit'), 'attr': { 'class': 'login' } }) }}
    {{ form_widget(form) }}
    <div>
        <input type="submit" class="btn btn-success btn-sm" name="_submit" value="{{ 'profile.edit.submit'|trans }}" />
    </div>
{{ form_end(form) }}
